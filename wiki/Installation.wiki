#labels Phase-Deploy
* These are the install instructions for my script:

 # download the file (for example with '*wget*')
 # make the file executable ('*chmod +x installer.sh*')
 # finally: run it! ('*./installer.sh*')

The commands are in the brackets.

Supported OS': *Debian 6*, *minimal* installation